# BC (bc_jax) shared hyperparameters.
# Dataset-specific fields (env, dataset_id, command_type, group) are injected
# per run from configs/offline/_datasets.yaml by the runner script.
project: Offline-Benchmark
name: BC
checkpoints_path: checkpoints/BC
device: cuda
seed: 0

batch_size: 256
buffer_size: 10000000
max_timesteps: 1000000
eval_freq: 5000
n_episodes: 10
normalize: false
discount: 0.99
frac: 1.0
max_traj_len: 1000
load_model: ''
